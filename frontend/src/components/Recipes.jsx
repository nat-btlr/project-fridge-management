import React from 'react';

const Recipes = ({ className }) => {
  // const [recipes, setRecipes] = useState([]);

  return (
    <div className={`${className} bg-green-100`}>
      <h1>Here will be the recipes</h1>
    </div>
  );
};

export default Recipes;
